ResponseObject:
  type: object
  required:
    - status
    - data
  properties:
    status:
      type: integer
      description: Status of the call
    data:
      type: string
      description: A string with the error message
